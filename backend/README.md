# Backend (Spring Boot)

- Replace `CombinedBackendApplication` package and add your modules.
- Add database, security, logging, and configuration per environment.
- Add unit and integration tests under src/test.
